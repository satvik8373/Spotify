{
  "name": "spotify-clone",
  "version": "1.0.0",
  "description": "Full-stack Spotify Clone with real-time features",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build": "cd frontend && npm run build && cd ../backend && npm run build"
  },
  "dependencies": {
    "@clerk/nextjs": "^4.23.2",
    "cloudinary": "^1.40.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
} 