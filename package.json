{
  "name": "spotify-clone",
  "version": "1.0.0",
  "description": "Spotify Clone with React and Node.js",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": ">=16.0.0"
  }
} 